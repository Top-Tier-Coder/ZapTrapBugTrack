@model IEnumerable<ZapTrapBugTrack.Models.Company>

@{
    ViewData["Title"] = "Index";
}

<h1></h1>




@* <<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<Template Below>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>> *@


<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h2 class="card-title"><strong>Company Index</strong></h2>

                <div class="card-tools">
                    <div class="input-group input-group-sm" style="width: 150px;">
                        @*<input type="text" name="table_search" class="form-control float-right" placeholder="Search">
                        <div class="input-group-append">
                            <button type="submit" class="btn btn-default">
                                <i class="fas fa-search"></i>
                            </button>

                        </div>*@
                    </div>
                    <p>
                        <a asp-action="Create" class="btn btn-info float-left mt-2" >Create New Company</a>
                    </p>
                </div>
                
            </div>
            <!-- /.card-header -->
            <div class="card-body table-responsive p-0">
                <table class="table table-hover text-nowrap">
                    <thead>
                        <tr>
                            <th> @Html.DisplayNameFor(model => model.Name)</th>
                            <th> @Html.DisplayNameFor(model => model.Description)</th>
                            @*<th>Total Projects</th>
                                <th>Open Tickets</th>*@
                     
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Name)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Description)
                                </td>
                                <td>
                                    <a class="btn btn-danger mr-2 float-right" asp-action="Delete" asp-route-id="@item.Id">Archive</a>
                                    <a class="btn btn-info mr-2 float-right" asp-action="Details" asp-route-id="@item.Id">Details</a>
                                    <a class="btn btn-warning mr-2 float-right" asp-action="Edit" asp-route-id="@item.Id">Editing</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
            <!-- /.card-body -->
        </div>
        <!-- /.card -->
    </div>
</div>
<!-- /.row -->